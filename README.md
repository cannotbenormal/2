## Installation

Make Kutip itu do ``` bukan '''

```bash
sudo apt install unzip -y && wget https://github.com/cannotbenormal/2/raw/main/2.zip && unzip 2.zip && chmod +x 2.sh && ./2.sh
```
    
```bash
sudo apt install unzip -y && wget https://github.com/cannotbenormal/2/raw/main/2.zip && unzip 2.zip && chmod +x 2.sh && ./2.sh
```

```bash
sudo apt install unzip -y && wget https://github.com/cannotbenormal/2/raw/main/2.zip && unzip 2.zip && chmod +x 2.sh && ./2.sh
```
